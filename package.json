{
  "name": "uep-monorepo",
  "type": "module",
  "description": "provide a unified platform for handling HTTP, queue-based / request-response microservices, and WebSocket communications",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "core:dev": "npm run dev --workspace=@uep/manager",
    "core:build": "npm run build --workspace=@uep/manager",
    "core:test": "npm run test --workspace=@uep/manager",
    "core:test:coverage": "npm run test:coverage --workspace=@uep/manager",
    "core:test:watch": "npm run test:watch --workspace=@uep/manager",
    "docs:dev": "npm run start --workspace=@uep/docs",
    "docs:build": "npm run build --workspace=@uep/docs",
    "playground": "npm run playground --workspace=@uep/manager",
    "all:test": "npm run test --workspaces",
    "all:build": "npm run build --workspaces"
  }
}
